services:
  homepage:
    image: ghcr.io/gethomepage/homepage:latest
    container_name: homepage
    ports:
      - 8080:3000
    volumes:
      - /home/garbhan/code/homelab/homepage/config:/app/config
      - /var/run/docker.sock:/var/run/docker.sock # (optional) For docker integrations

